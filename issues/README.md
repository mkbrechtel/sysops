# Issues

We track issues as `.md` files in this directory. Feature tickets use the
`.feature.md` suffix and follow a loose structure: **Goal → Scope → Design
notes → Open questions**.

Once an issue is implemented, its file moves to `docs/features/` and is
featured prominently on the project homepage.
